package org.javavitamins.modules.logger;

public class LogFactory {

	/*// singletn instance
	private static LogFactory logFactory = new LogFactory();

	public static LogFactory getInstance() {
		return logFactory;
	}

	public ArchitectureToolkitIF getLogger(int architecture) {
		switch (architecture) {
			case ENGINOLA :
				return new EnginolaToolkit();

			case EMBER :
				return new EmberToolkit();
		} // switch
		String errMsg = Integer.toString(architecture);
		throw new IllegalArgumentException(errMsg);
	}*/

}
